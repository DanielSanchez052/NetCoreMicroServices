apiVersion: v1
kind: PersistentVolumeClaim
metadata: 
  name: mssql-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: local-storage-class 
  resources:
    requests:
      storage: 100Mi
